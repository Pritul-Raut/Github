name: GitHub Action with Email

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
    - name: ppppp@gmail.com
      uses: actions/checkout@v3

    - name: Step with Email
      run: echo "This step is named with the email address"

    - name: Validate JSON Content
      run: |
        echo "Validating JSON content..."
        echo "This is a placeholder for JSON validation logic."
